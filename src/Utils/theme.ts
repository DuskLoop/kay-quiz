import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#f2f2f2",
    },
  },
  typography: {
    fontFamily: '"M PLUS Rounded 1c", "Helvetica", "Arial", sans-serif',
  },
});
